<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug overrides for development - these don't trigger hostname-aware validation -->
    <!-- Only active when android:debuggable="true" in AndroidManifest -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.1.6</domain>
        <domain includeSubdomains="false">wss://nos.lol</domain>
    </domain-config>
</network-security-config>
